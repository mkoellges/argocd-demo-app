apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: example-app

resources:
  - ../../base
  - namespace.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
- name: example-app
  literals:
    - STAGE="DEV"

namespace: example-app-dev
